[defaults]
# Applied automatically when running ansible-playbook from the ansible/ directory
inventory = inventory/azure.ini
host_key_checking = False
# No roles_path needed: roles live in playbooks/roles/, found next to the playbook automatically
