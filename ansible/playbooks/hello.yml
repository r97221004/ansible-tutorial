---
- name: Hello World
  hosts: control
  tasks:
    - name: 印出訊息
      debug:
        msg: "Ansible 可以動了！這台機器是 {{ ansible_hostname }}"

    - name: 確認作業系統
      debug:
        msg: "OS: {{ ansible_distribution }} {{ ansible_distribution_version }}"
