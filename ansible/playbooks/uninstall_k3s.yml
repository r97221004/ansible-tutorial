---
# Uninstall the K3s cluster (leaves k9s in place; run uninstall_kube_tools.yml to remove tooling)
# Usage: ansible-playbook playbooks/uninstall_k3s.yml
- name: Uninstall K3s cluster
  hosts: control
  become: true
  roles:
    - role: k3s
      k3s_state: absent
