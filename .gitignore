# Ansible
*.retry
*.log

# Vault password files (never commit secrets)
.vault_pass
.vault_pass.txt
vault-password*
*.vault_pass

# Vault files (create locally with: ansible-vault create)
**/group_vars/**/vault.yml

# Local inventory / secret overrides
**/host_vars/*.local.yml
**/group_vars/*/*.local.yml

# Python virtualenv
.venv/
venv/

# Editor / OS
.vscode/
.idea/
*.swp
*~
.DS_Store
